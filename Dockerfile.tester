FROM sqlite_builder

WORKDIR /sqlite

# Create a non-root user
RUN groupadd -r testuser && useradd -r -g testuser testuser

# Change ownership of the sqlite directory to the test user
RUN chown -R testuser:testuser /sqlite

# Switch to non-root user
USER testuser

RUN make test
